<Window x:Class="DeskBuddy.IndexProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        Topmost="True" ShowInTaskbar="False" ShowActivated="False"
        SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        MouseLeftButtonDown="OnDismiss" Loaded="OnLoaded">
    <Border CornerRadius="12" Padding="14,10" Width="360"
            Background="#F21B1B1B" BorderBrush="#40FFFFFF" BorderThickness="1"
            MouseLeftButtonDown="OnDismiss">
        <StackPanel>
            <StackPanel Orientation="Horizontal">
                <TextBlock x:Name="TitleText" Text="DeskBuddy 正在建立文件索引" FontSize="13" FontWeight="SemiBold"
                           Foreground="White" VerticalAlignment="Center"/>
                <TextBlock x:Name="CloseGlyph" Text="&#xE711;" FontFamily="Segoe MDL2 Assets" FontSize="11"
                           Foreground="#99FFFFFF" Margin="8,0,0,0" VerticalAlignment="Center"
                           ToolTip="点击任意处关闭"/>
            </StackPanel>
            <TextBlock x:Name="DetailText" Text="" FontSize="11" Foreground="#D0FFFFFF"
                       Margin="0,4,0,8" TextTrimming="CharacterEllipsis"/>
            <ProgressBar x:Name="Progress" Height="8" Minimum="0" Maximum="100" Value="0"
                         Foreground="#4CAF50" Background="#33FFFFFF" BorderThickness="0"/>
            <TextBlock x:Name="PercentText" Text="正在统计…" FontSize="10" Foreground="#AAFFFFFF" Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</Window>
